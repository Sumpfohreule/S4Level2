########################################################################################################################
is.URI <- function(x) {
    is_object_an_uri <- inherits(x, "URI")
    return(is_object_an_uri)
}
