########################################################################################################################
is.Logger <- function(x) {
	is_logger <- inherits(x, what = "Logger")
    return(is_logger)
}
